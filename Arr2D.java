
public class Arr2D {

	public static void main(String[] args) {
		// TODO Auto-generated method stub
		/*
		int[][] Scores = new int[][]{{85,80,70,90},{70,100,90,60},{100,100,80,75}};
		
		
		for (int i = 0 ; i < Scores.length ; i++)
		{
			int Sum = 0;
			
			for (int j = 0 ; j < Scores[i].length ; j++)
			{
				Sum += Scores[i][j];
			}
			System.out.println("row " + i + " : " + (double) Sum / Scores[i].length);
		}
		
		for (int i = 0 ; i < Scores[0].length ; i++)
		{
			int Sum = 0 ;
			
			for (int j = 0 ; j < Scores.length ; j++)
			{
				Sum += Scores[j][i];
			}
			
			System.out.println("Column " + i + " : " + (double)Sum / Scores.length);
			
		}


		
		
		
		
		
		
		
		
		*/
	}

}
